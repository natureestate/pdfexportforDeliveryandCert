# 🤖 Google Agent Development Kit (ADK) - คู่มือการใช้งานกับ MCP Context7

## 📖 Google ADK คืออะไร?

**Google Agent Development Kit (ADK)** เป็นเฟรมเวิร์กโอเพนซอร์สที่พัฒนาโดย Google Cloud เพื่อช่วยให้องค์กรสามารถสร้างและปรับใช้ระบบเอเจนต์ AI ได้อย่างมีประสิทธิภาพ

### ✨ คุณสมบัติหลักของ ADK

1. **Flexible Orchestration** - จัดการและประสานงานระหว่างเอเจนต์ต่าง ๆ ได้อย่างยืดหยุ่น
2. **Multi-Agent Architecture** - รองรับการพัฒนาระบบที่มีหลายเอเจนต์ทำงานร่วมกัน
3. **Rich Tool Ecosystem** - เชื่อมต่อกับเครื่องมือและทรัพยากรภายนอกผ่าน **Model Context Protocol (MCP)** ได้อย่างสะดวก
4. **Deployment Ready** - มีเครื่องมือสำหรับการทดสอบและติดตั้งเอเจนต์ พร้อมทั้ง Web UI
5. **Built-in Evaluation** - มีระบบประเมินผลการทำงานของเอเจนต์ในตัว
6. **Security** - มุ่งเน้นความปลอดภัยในการพัฒนาและปรับใช้เอเจนต์

---

## 🔗 การเชื่อมต่อกับ MCP Context7

### MCP Context7 คืออะไร?

**Context7** เป็น MCP Server ที่ช่วยให้ AI สามารถเข้าถึงเอกสารและข้อมูลจาก library ต่าง ๆ ได้อย่างง่ายดาย โดยใช้ Context7-compatible library IDs

### การใช้งาน ADK กับ Context7

ADK รองรับ **Model Context Protocol (MCP)** ซึ่งช่วยให้เอเจนต์สามารถ:
- เข้าถึงเอกสารและข้อมูลจาก library ต่าง ๆ
- ใช้ข้อมูลจาก Context7 เพื่อเพิ่มความแม่นยำในการตอบคำถาม
- เชื่อมต่อกับเครื่องมือภายนอกได้อย่างง่ายดาย

---

## 🚀 การติดตั้งและเริ่มต้นใช้งาน

### ติดตั้ง Google ADK

#### Python

```bash
pip install google-adk
```

#### Java

**Maven (pom.xml):**
```xml
<dependency>
    <groupId>com.google.adk</groupId>
    <artifactId>google-adk</artifactId>
    <version>0.2.0</version>
</dependency>
```

**Gradle (build.gradle):**
```groovy
dependencies {
    implementation 'com.google.adk:google-adk:0.2.0'
}
```

### ตั้งค่า MCP Context7 ใน Cursor

ไฟล์ `mcp.json` ของคุณมี Context7 ตั้งค่าไว้แล้ว:

```json
{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": [
        "-y",
        "@upstash/context7-mcp"
      ]
    }
  }
}
```

---

## 💡 ตัวอย่างการใช้งาน ADK กับ Context7

### ตัวอย่างที่ 1: สร้างเอเจนต์ที่ใช้ข้อมูลจาก Context7

```python
from google.adk import Agent, Tool
from context7 import get_library_docs

# สร้าง Tool สำหรับดึงข้อมูลจาก Context7
def get_documentation(library_id: str, topic: str = None):
    """ดึงเอกสารจาก Context7 library"""
    docs = get_library_docs(
        context7CompatibleLibraryID=library_id,
        topic=topic,
        tokens=5000
    )
    return docs

# สร้างเอเจนต์
agent = Agent(
    name="Documentation Helper",
    goal="ช่วยค้นหาและอธิบายเอกสารจาก library ต่าง ๆ",
    tools=[
        Tool(
            name="get_docs",
            description="ดึงเอกสารจาก Context7 library",
            function=get_documentation
        )
    ]
)

# ใช้งานเอเจนต์
response = agent.run("อธิบายวิธีการใช้ Firebase Admin SDK")
print(response)
```

### ตัวอย่างที่ 2: ใช้ ADK กับ Google Agentspace

Google Agentspace เป็นแพลตฟอร์ม cloud-based สำหรับสร้างและจัดการเอเจนต์ AI

```python
from google.adk import AgentspaceAgent

# สร้างเอเจนต์ใน Agentspace
agent = AgentspaceAgent(
    name="Firebase Helper",
    goal_description="ช่วยเหลือเกี่ยวกับ Firebase",
    instructions="""
    คุณเป็นผู้ช่วยที่เชี่ยวชาญเกี่ยวกับ Firebase
    ใช้ Context7 เพื่อดึงเอกสารล่าสุดจาก Firebase library
    และตอบคำถามอย่างละเอียดและถูกต้อง
    """,
    data_sources=["Firebase Documentation"],
    tools=["context7", "firebase-cli"]
)

# ใช้งานเอเจนต์
result = agent.assist("อธิบายวิธีการใช้ Firebase Authentication")
```

---

## 🎯 ประเภทของเอเจนต์ใน ADK

### 1. LLM Agent
เอเจนต์ที่ใช้ Large Language Model เป็นหลัก

```python
from google.adk import LLMAgent

agent = LLMAgent(
    model="gemini-pro",
    tools=[get_documentation]
)
```

### 2. Sequential Agent
เอเจนต์ที่ทำงานแบบเรียงลำดับ

```python
from google.adk import SequentialAgent

agent = SequentialAgent(
    steps=[
        "ค้นหาข้อมูลจาก Context7",
        "วิเคราะห์ข้อมูล",
        "สรุปผลลัพธ์"
    ]
)
```

### 3. Parallel Agent
เอเจนต์ที่ทำงานหลายงานพร้อมกัน

```python
from google.adk import ParallelAgent

agent = ParallelAgent(
    tasks=[
        "ค้นหาข้อมูล Firebase",
        "ค้นหาข้อมูล Google Cloud",
        "ค้นหาข้อมูล Vertex AI"
    ]
)
```

### 4. Loop Agent
เอเจนต์ที่ทำงานวนซ้ำจนกว่าจะได้ผลลัพธ์ที่ต้องการ

```python
from google.adk import LoopAgent

agent = LoopAgent(
    condition=lambda result: result.confidence > 0.9,
    max_iterations=5
)
```

---

## 🔧 การใช้งาน Context7 ใน ADK

### ขั้นตอนการใช้งาน

1. **Resolve Library ID** - ค้นหา library ที่ต้องการ

```python
from context7 import resolve_library_id

# ค้นหา library
results = resolve_library_id("firebase")
firebase_lib_id = results[0].library_id  # เช่น /websites/firebase_google
```

2. **Get Library Documentation** - ดึงเอกสารจาก library

```python
from context7 import get_library_docs

# ดึงเอกสาร Firebase
docs = get_library_docs(
    context7CompatibleLibraryID="/websites/firebase_google",
    topic="authentication",
    tokens=5000
)
```

3. **ใช้ข้อมูลในเอเจนต์**

```python
agent = Agent(
    name="Firebase Expert",
    context=docs,  # ใช้เอกสารเป็น context
    tools=[firebase_tools]
)
```

---

## 📚 ตัวอย่างเอเจนต์ที่สร้างจาก ADK

### 1. Academic Research Agent
เอเจนต์ผู้ช่วยด้านวิชาการ สำหรับค้นหาข้อมูลงานวิจัยและเอกสาร

```python
agent = Agent(
    name="Academic Research Assistant",
    goal="ช่วยค้นหาและสรุปงานวิจัย",
    tools=[context7_research_tools]
)
```

### 2. Blog Writer Agent
เอเจนต์สำหรับเขียนบล็อก

```python
agent = Agent(
    name="Blog Writer",
    goal="เขียนบล็อกที่มีคุณภาพ",
    tools=[context7_writing_tools]
)
```

### 3. Customer Service Agent
เอเจนต์สำหรับบริการลูกค้า

```python
agent = Agent(
    name="Customer Service",
    goal="ให้บริการลูกค้าอย่างดี",
    tools=[context7_knowledge_base]
)
```

### 4. Google Trend Agent
เอเจนต์สำหรับค้นหาเทรนด์บน Google

```python
agent = Agent(
    name="Trend Analyzer",
    goal="วิเคราะห์เทรนด์จาก Google",
    tools=[google_trends_api]
)
```

---

## 🛠️ การ Deploy เอเจนต์

### Deploy บน Vertex AI

```python
from google.adk import deploy_to_vertex

deploy_to_vertex(
    agent=agent,
    project_id="your-project-id",
    region="us-central1"
)
```

### Deploy บน Google Agentspace

```python
from google.adk import deploy_to_agentspace

deploy_to_agentspace(
    agent=agent,
    collection_id="your-collection-id"
)
```

---

## 🔐 Security Best Practices

### ✅ ควรทำ

1. **ใช้ Service Account** - สำหรับ production deployment
2. **จำกัด Permissions** - ให้สิทธิ์เฉพาะที่จำเป็น
3. **ใช้ Environment Variables** - สำหรับ credentials
4. **Monitor Usage** - ติดตามการใช้งานเอเจนต์

### ❌ ไม่ควรทำ

1. **ไม่ควร hard-code credentials** - ในโค้ด
2. **ไม่ควรเปิด API key สาธารณะ** - ให้ทุกคนเข้าถึงได้
3. **ไม่ควรใช้ user credentials** - ใน production

---

## 📖 เอกสารเพิ่มเติม

- [Google ADK GitHub](https://github.com/google/adk)
- [Google Agentspace Documentation](https://cloud.google.com/agentspace/docs)
- [Model Context Protocol](https://modelcontextprotocol.io/)
- [Context7 Documentation](https://context7.io/)

---

## 🎯 สรุป

**Google ADK** ช่วยให้คุณสามารถ:

1. ✅ สร้างเอเจนต์ AI ได้อย่างง่ายดาย
2. ✅ เชื่อมต่อกับ Context7 เพื่อดึงข้อมูล
3. ✅ จัดการ workflow ที่ซับซ้อน
4. ✅ Deploy เอเจนต์ได้อย่างรวดเร็ว
5. ✅ ประเมินผลการทำงานของเอเจนต์

**การใช้งานกับ Context7:**
- ใช้ Context7 เพื่อดึงเอกสารจาก library ต่าง ๆ
- เพิ่มความแม่นยำในการตอบคำถาม
- เชื่อมต่อกับเครื่องมือภายนอกได้ง่ายขึ้น

---

## 🚀 เริ่มต้นใช้งานตอนนี้!

```bash
# ติดตั้ง ADK
pip install google-adk

# ตั้งค่า Context7 ใน mcp.json (ทำแล้ว)
# เริ่มสร้างเอเจนต์ของคุณ!
```

**Happy Coding! 🎉**

